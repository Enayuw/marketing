<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.SmsVendorInfoNormalMapper" >

    <select id="selectCount" resultType="java.lang.Long">
        SELECT
            count(*)
        FROM
            b_marketing_sms_vendor_info_normal
        WHERE
            vendor_id =#{vendorId} and vendor_name=#{vendorName} and ope_status in (0,2) and is_delete =0
    </select>


    <update id="updateInfoById">
        UPDATE
            b_marketing_sms_vendor_info_normal
        SET vendor_name = #{vendorName},ope_status=#{opeStatus},ope_time=now()
        WHERE
            id = #{vendorPrimaryId}
    </update>

</mapper>