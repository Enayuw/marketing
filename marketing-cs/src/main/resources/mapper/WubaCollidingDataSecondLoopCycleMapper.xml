<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.WubaCollidingDataSecondLoopCycleMapper" >
    <select id="selectCollidingData" resultType="com.br.marketing.entity.WubaCollidingData">
        select id, cell
        from b_wuba_colliding_data_second_loop_cycle
        where is_deleted = 0
          and api_code = #{apiCode}
          <![CDATA[ and push_time >= #{pushTimeStart} and push_time < #{pushTimeEnd} ]]>
        order by push_time
            limit #{pageSize}
    </select>

    <update id="batchUpdatePushTimeById">
        update b_wuba_colliding_data_loop_cycle set push_time = now()
        where id in
        <foreach collection="datas" item="data" open="(" close=")" separator=",">
            #{data.id}
        </foreach>
    </update>
</mapper>