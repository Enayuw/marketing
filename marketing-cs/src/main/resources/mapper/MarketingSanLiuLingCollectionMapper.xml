<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.MarketingSanLiuLingCollectionMapper" >

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO b_sanliuling_collection_details
        (api_code, task_id, batch_no, application_id, phone,
        speech_param_set, customer_name, case_code, product_type, prologue_remark,
        phone_label, clean_status, is_delete, receive_date, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.apiCode}, #{item.taskId}, #{item.batchNo}, #{item.applicationId}, #{item.phone},
            #{item.speechParamSet}, #{item.customerName}, #{item.caseCode}, #{item.productType}, #{item.prologueRemark},
            #{item.phoneLabel}, #{item.cleanStatus}, #{item.isDelete}, #{item.receiveDate} , #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

    <!-- 分页查询不重复的applicationId列表 -->
    <select id="selectDistinctApplicationIdsWithPaging" resultType="java.lang.String">
        SELECT application_id
        FROM (
            SELECT DISTINCT application_id
            FROM b_sanliuling_collection_details
            WHERE api_code = #{apiCode}
              AND receive_date = #{receiveDate}
              AND clean_status = #{cleanStatus}
              AND is_delete = 0
              AND application_id IS NOT NULL
              AND application_id != ''
            ORDER BY application_id
        ) t
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 查询不重复applicationId的总数量 -->
    <select id="countDistinctApplicationIds" resultType="java.lang.Long">
        SELECT COUNT(DISTINCT application_id)
        FROM b_sanliuling_collection_details
        WHERE api_code = #{apiCode}
          AND receive_date = #{receiveDate}
          AND clean_status = #{cleanStatus}
          AND is_delete = 0
          AND application_id IS NOT NULL
          AND application_id != ''
    </select>

    <!-- 根据applicationId查询所有相关数据 -->
    <select id="selectByApplicationId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM b_sanliuling_collection_details
        WHERE api_code = #{apiCode}
          AND receive_date = #{receiveDate}
          AND clean_status = #{cleanStatus}
          AND application_id = #{applicationId}
          AND is_delete = 0
    </select>

    <!-- 根据applicationId批量更新cleanStatus状态 -->
    <update id="updateCleanStatusByApplicationId">
        UPDATE b_sanliuling_collection_details
        SET clean_status = #{newCleanStatus},
            update_time = NOW()
        WHERE api_code = #{apiCode}
          AND receive_date = #{receiveDate}
          AND application_id = #{applicationId}
          AND clean_status = #{oldCleanStatus}
          AND is_delete = 0
    </update>

</mapper>