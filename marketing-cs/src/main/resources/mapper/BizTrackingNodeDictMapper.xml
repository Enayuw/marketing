<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.BizTrackingNodeDictMapper" >

    <!-- 检查节点是否存在 -->
    <select id="checkNodeExists" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM biz_tracking_node_dict
        WHERE node_code = #{nodeCode}
          AND api_code = #{apiCode}
          AND node_type = #{nodeType}
            LIMIT 1
    </select>

    <!-- 更新节点的最后出现时间（更新 update_time） -->
    <update id="updateLastSeenTime">
        UPDATE biz_tracking_node_dict
        SET update_time = NOW(),
            is_active = 1
        WHERE node_code = #{nodeCode}
          AND api_code = #{apiCode}
          AND node_type = #{nodeType}
    </update>

    <!-- 根据API代码查询节点列表 -->
    <select id="selectByApiCode" resultType="com.br.marketing.entity.BizTrackingNodeDict">
        SELECT
            id,
            api_code AS apiCode,
            node_code AS nodeCode,
            node_type AS nodeType,
            node_name AS nodeName,
            node_desc AS nodeDesc,
            is_active AS isActive,
            create_time AS createTime,
            update_time AS updateTime
        FROM biz_tracking_node_dict
        WHERE api_code = #{apiCode}
          AND is_active = 1
        ORDER BY update_time DESC
    </select>

</mapper>