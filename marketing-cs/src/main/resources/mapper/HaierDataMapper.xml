<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.HaierDataMapper" >
    <select id="selectDataLimitId" resultType="com.br.marketing.entity.HaierData">
        select * from b_haier_data
        where local_id = #{localId}
        and status = 1
        and push_status = 1
        <if test="minId !=null and minId>0">
            and create_date=#{minId}
        </if>
        order by id asc
        limit 5000;
    </select>

    <select id="selectMinId" resultType="java.lang.Long">
        select min(id) from b_haier_data
        where local_id = #{localId}
        and status = 1;
    </select>
</mapper>