<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.TaikangDingDingTransferDetailMapper" >

    <select id="selectDetailList" resultType="com.br.marketing.entity.TaikangDingDingTransferDetail">
        SELECT
            id, cell, browse_date, application_name, return_result1, extend, push_status, create_time,
            update_time, `status`, error_msg, api_code
        FROM b_dd_taikang_transfer_detail
        WHERE
            id > #{searchId}
            AND status = 1
            AND push_status = #{pushStatus}
        ORDER BY id ASC
        LIMIT #{searchSize}
    </select>
</mapper>