<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.DataDistributeDetailLogMapper" >

    <select id="getToDataDistributeInfoList" resultType="java.lang.String">
        select
         cust_num
        from b_data_distribute_detail_log
        where api_code = #{apiCode}
        and cust_num in
        <foreach collection="custNums" item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
        and distribute_date between  DATE_FORMAT(now(),"%Y-%m-%d") and DATE_add(DATE_FORMAT(now(),"%Y-%m-%d"),INTERVAL 1 day)
    </select>
</mapper>