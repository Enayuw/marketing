<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.MarketingTcyrCpaSyncRecordMapper" >
    <update id="updateTcyrRecordDownStatus">
        UPDATE b_tcyr_cpa_sync_record
        SET down_status = #{downStatus}
        WHERE id = #{id}
    </update>

    <select id="searchTcyrSyncRecordList" resultType="com.br.marketing.entity.MarketingTcyrCpaSyncRecord">
        SELECT  *
        FROM b_tcyr_cpa_sync_record
        WHERE api_code = #{apiCode}
          AND status = #{status}
          AND down_status = 0
          AND is_del = 1
    </select>
</mapper>