<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.MarketingHaLuoCallBackDataMapper" >


    <select id="selectDataList" resultType="com.br.marketing.entity.MarketingHaloCallBackData">
        SELECT id, api_code, cell, cust_num, user_type, start_time, deal_status, `status`, is_del,
               create_time, update_time, extend
        FROM b_marketing_halo_callback_data
        WHERE
            api_code = #{apiCode}
            AND deal_status = #{dealStatus}
        <if test="startSearchTime != null">
            AND create_time >= #{startSearchTime}
        </if>
        Limit #{searchSeize}
    </select>

    <update id="updateDealStatusByIdList">
        UPDATE b_marketing_halo_callback_data
        SET deal_status = #{dealStatus}
          <if test="extend != null and extend != ''">
            , extend = #{extend}
          </if>
        WHERE id in (
            <foreach collection="idList" item="item" index="index" separator=",">
                #{item}
            </foreach>
        )
    </update>


</mapper>