<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.MarketingTcyrSyncRecordMapper" >

    <select id="searchAllTcyrSyncList" resultType="com.br.marketing.entity.MarketingTcyrSyncRecord">
        SELECT  id, api_code, request_no, batch_no, scene, `data`, `status`, assign_status, msg, create_time, update_time,down_status,is_del
        FROM b_marketing_tcyr_sync_record
        WHERE api_code = #{apiCode}
          AND status = #{status}
          AND is_del = 1
    </select>

    <select id="searchTcyrSyncList" resultType="com.br.marketing.entity.MarketingTcyrSyncRecord">
        SELECT  id, api_code, request_no, batch_no, scene, `data`, `status`, assign_status, msg, create_time, update_time,down_status,is_del
        FROM b_marketing_tcyr_sync_record
        WHERE api_code = #{apiCode}
          AND status = #{status}
          AND down_status = 0
          AND is_del = 1
    </select>

    <!-- 拆 apiCode 后：仅对已归因 api_code 的记录拉取文件（标准链路需先 fill） -->
    <select id="searchTcyrSyncListApiCodeNotNull" resultType="com.br.marketing.entity.MarketingTcyrSyncRecord">
        SELECT  id, api_code, request_no, batch_no, scene, `data`, `status`, assign_status, msg, create_time, update_time,down_status,is_del
        FROM b_marketing_tcyr_sync_record
        WHERE api_code IS NOT NULL
          AND status = #{status}
          AND down_status = 0
          AND is_del = 1
    </select>

    <select id="searchAllTcyrSyncListApiCodeNotNull" resultType="com.br.marketing.entity.MarketingTcyrSyncRecord">
        SELECT  id, api_code, request_no, batch_no, scene, `data`, `status`, assign_status, msg, create_time, update_time,down_status,is_del
        FROM b_marketing_tcyr_sync_record
        WHERE api_code IS NOT NULL
          AND status = #{status}
          AND is_del = 1
    </select>

    <select id="selectLatestSceneByBatchNo" resultType="java.lang.String">
        SELECT scene
        FROM b_marketing_tcyr_sync_record
        WHERE api_code = #{apiCode}
          AND batch_no = #{batchNo}
          AND is_del = 1
        ORDER BY id DESC
        LIMIT 1
    </select>

    <select id="selectLatestByBatchNo" resultType="com.br.marketing.entity.MarketingTcyrSyncRecord">
        SELECT id, api_code, request_no, batch_no, scene, `data`, `status`, assign_status, msg, create_time, update_time, down_status, is_del
        FROM b_marketing_tcyr_sync_record
        WHERE batch_no = #{batchNo}
          AND is_del = 1
        ORDER BY id DESC
        LIMIT 1
    </select>

    <select id="selectPendingApiCodeMatchRecords" resultType="com.br.marketing.entity.MarketingTcyrSyncRecord">
        SELECT id, api_code, request_no, batch_no, scene, `data`, `status`, assign_status, msg, create_time, update_time, down_status, is_del
        FROM b_marketing_tcyr_sync_record
        WHERE api_code IS NULL
          AND `status` = 1
          AND assign_status IS NULL
          AND is_del = 1
        ORDER BY id ASC
        LIMIT #{limit}
    </select>

    <update id="claimAssignCardDispatched">
        UPDATE b_marketing_tcyr_sync_record
        SET assign_status = 1,
            update_time = NOW()
        WHERE id = #{id}
          AND assign_status IS NULL
          AND api_code IS NULL
          AND `status` = 1
          AND is_del = 1
    </update>

    <update id="resetAssignStatusAfterFailedDispatch">
        UPDATE b_marketing_tcyr_sync_record
        SET assign_status = NULL,
            update_time = NOW()
        WHERE id = #{id}
          AND assign_status = 1
          AND api_code IS NULL
    </update>

    <select id="countTodayByApiCodeAndBatchPrefix" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM b_marketing_tcyr_sync_record
        WHERE api_code = #{apiCode}
          AND (
            batch_no = #{batchPrefix}
            OR batch_no LIKE CONCAT(#{batchPrefix}, '\_%')
          )
          AND DATE(create_time) = CURRENT_DATE()
          AND is_del = 1
    </select>


    <update id="updateTcyrRecordDownStatus">
        update b_marketing_tcyr_sync_record set down_status = #{downStatus} where batch_no = #{batchNo}
    </update>


    <insert id="batchAdd">
        INSERT INTO marketing.b_marketing_tcyr_sync
        (api_code,batch_no,user_key,terminal,create_time,update_time,status,extend)
        values
         <foreach collection="dataList" item="item" separator=",">
             (
              #{item.apiCode},#{item.batchNo},#{item.userKey},#{item.terminal},
              #{item.createTime},#{item.updateTime},#{item.status},#{item.extend}
             )
         </foreach>
    </insert>

    <select id="selectLastUserRecordIdList" resultType="java.lang.Long">
        SELECT  MAX(id)
        FROM b_marketing_sync_${apiCode}
        WHERE cust_num IN
        <foreach item="userKey" index="index" collection="userKeyList"
                 open="(" separator="," close=")">
            #{userKey}
        </foreach>
        GROUP BY cust_num;
    </select>


    <select id="selectLastCustNumCelltikv_" resultType="java.util.Map">
        SELECT
            cust_num AS custNum,
            cell_md5 AS cell
        FROM (
            SELECT
                cust_num,
                cell_md5,
                ROW_NUMBER() OVER (PARTITION BY cust_num ORDER BY applet_time DESC) AS rn
            FROM b_marketing_sync_${apiCode}
                WHERE status = 1 AND cust_num IN
                <foreach collection="userKeyList" item="userKey" open="(" separator="," close=")">
                    #{userKey}
                </foreach>
        ) t
        WHERE rn = 1
    </select>


    <select id="selectSingleLastCustNumCelltikv_" resultType="java.lang.String">
        SELECT
            cell_md5 AS cell
        FROM
            b_marketing_sync_${apiCode}
        WHERE status = 1
              AND cust_num =#{userKey}
              AND is_repeat in (1,2)
        ORDER BY applet_time DESC
        LIMIT 1
    </select>

</mapper>