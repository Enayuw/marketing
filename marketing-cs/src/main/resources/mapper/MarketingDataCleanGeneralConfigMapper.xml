<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.br.marketing.mapper.rulecleaning.MarketingDataCleanGeneralConfigMapper">
    
    <!-- 根据条件查询规则列表 -->
    <select id="selectRuleList" parameterType="com.br.marketing.entity.MarketingDataCleanGeneralConfig" resultMap="com.br.marketing.mapper.MarketingDataCleanGeneralConfigMapperBase.BaseResultMap">
        SELECT 
            a.*
        FROM 
            b_marketing_data_clean_general_config a
        WHERE 
            a.is_del = 1
        <if test="apiCode != null and apiCode != ''">
            AND a.api_code = #{apiCode,jdbcType=VARCHAR}
        </if>
        <if test="accountType != null and accountType != ''">
            AND a.account_type = #{accountType,jdbcType=VARCHAR}
        </if>
        <if test="acceptType != null">
            AND a.accept_type = #{acceptType,jdbcType=INTEGER}
        </if>
        ORDER BY a.update_time DESC
    </select>
    
    <!-- 统计符合条件的规则数量 -->
    <select id="countRuleList" parameterType="com.br.marketing.entity.MarketingDataCleanGeneralConfig" resultType="java.lang.Long">
        SELECT 
            COUNT(1)
        FROM 
            b_marketing_data_clean_general_config a
        WHERE 
            a.is_del = 1
        <if test="apiCode != null and apiCode != ''">
            AND a.api_code = #{apiCode,jdbcType=VARCHAR}
        </if>
        <if test="accountType != null and accountType != ''">
            AND a.account_type = #{accountType,jdbcType=VARCHAR}
        </if>
        <if test="acceptType != null">
            AND a.accept_type = #{acceptType,jdbcType=INTEGER}
        </if>
    </select>

</mapper> 