<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.MarketingTcyrCustCellMappingMapper" >

    <insert id="batchSaveCustCell">
        ${insertSql}
    </insert>


    <insert id="saveNewCustCellInfo">
        INSERT INTO
            b_marketing_tcyr_cust_cell_mapping (cust_num,cell,update_time)
        VALUES
            (#{userKey},#{cell},NOW())
        ON DUPLICATE KEY UPDATE
            cell = VALUES(cell),
            update_time = NOW()
    </insert>


  <select id="selectCelltikv_" resultType="java.lang.String">
      select
          cell
      from  b_marketing_tcyr_cust_cell_mapping
      where id = #{userKeyId}
    </select>

    <select id="selectCellInfotiflash_" resultType="java.util.Map">
        SELECT
            cust_num AS custNum,
            cell AS cell
        FROM b_marketing_tcyr_cust_cell_mapping
        WHERE cust_num IN
        <foreach collection="userKeyList" item="userKey" open="(" separator="," close=")">
            #{userKey}
        </foreach>
    </select>

    <select id="selectCellInfotikv_" resultType="java.util.Map">
        SELECT
        id AS custNum,
        cell AS cell
        FROM b_marketing_tcyr_cust_cell_mapping
        WHERE id IN
        <foreach collection="userKeyList" item="userKey" open="(" separator="," close=")">
            #{userKey}
        </foreach>
    </select>

</mapper>