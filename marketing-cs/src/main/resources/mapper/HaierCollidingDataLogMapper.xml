<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.HaierCollidingDataLogMapper">
    <insert id="saveBatchLog" parameterType="list">
        insert into b_haier_colliding_data_log (api_code, local_id, colliding_data_id,
                                                          type, mobile_digest, status, send_date, create_time, update_time)values
        <foreach collection="list" item="data" separator=",">
            (#{data.apiCode,jdbcType=VARCHAR}, #{data.localId,jdbcType=BIGINT}, #{data.collidingDataId,jdbcType=BIGINT},
             #{data.type,jdbcType=VARCHAR}, #{data.mobileDigest,jdbcType=VARCHAR},
             #{data.status,jdbcType=INTEGER}, #{data.sendDate,jdbcType=VARCHAR}, now(), now())
        </foreach>
    </insert>

    <update id="updateBySelective" parameterType="map" >
        update b_haier_colliding_data_log
        <set >
            <if test="result != null" >
                result = #{result,jdbcType=BIT},
            </if>
            <if test="status != null" >
                status = #{status,jdbcType=INTEGER},
            </if>
        </set>
        where mobile_digest = #{mobileDigest,jdbcType=VARCHAR}
        and send_date = #{sendDate} and status = 1
    </update>
</mapper>