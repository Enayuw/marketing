# 数据链路追踪

## 一、系统介绍
    智能营销中台，其主要的作用有三个。第一是承接客户的数据。第二是把客户的数据，根据系统产生的数据，进行逻辑判断，然后把数据推送给下游系统。

## 一、现有的业务数据的含义及分类
- 代运营数据——客户委托给我们去运营的名单数据
- 跑批数据——调用我司自有的评分或者数据产品，对客户的代运营名单数据在金融场景的行为和数据输出一些指标
- 转化数据——客户侧名单的数据发生一些行为变化的数据
- 通话明细数据——我司自有ai通信的通话记录明细数据
- 定制客户的定制接口数据——客户要求我们按照他们自有的接口标准开发接口，用于接受代运营数据
- 客户的接口数据——调客户接口获取到代运营数据的一些补充的字段信息

## 二、程序进行数据接收和逻辑计算场景
- 接口
- 作业
- 通过中间件的生产者和消费者

## 三、业务数据的流转过程
```mermaid
 graph TB
    A[代运营数据上传接口] --数据入库--> B[代运营数据]
    
    C[跑批作业] --获取数据--> B --调用跑批接口--> D[结果数据写入ES和doris]

    F[筛选推送作业] --获取数据--> D --按照规则筛选推送--> G[决策(下游系统)]

    H[转化数据上传接口] --> I[数据入库] --> J[MQ消费队列] --> k[消费端] --> M{逻辑判断方法}
    M --> |符合已转化条件| N[推送外呼系统的过滤接口]
    M --> |符合黑名单条件| O[推送外呼系统的过滤接口]
    M --> |符合人工条件| P[推送DAAS系统的人工接口]
    M --> |符合决策条件| G
    M --> |不符合条件| Q[结束]

    R[定制作业] --获取数据--> B --调用客户接口--> S[客户接口返回的数据] --> T[逻辑判断] --> G  

```

以上是5个业务流程的简版的流程图,复杂性如下：
- A,H 这样的接口都是批量接口（量级都是亿级别）
- C,F,R这样的作业都是大批量数据的作业（量级都是亿级别）
- R这样的定制作业还有很多
- M，T这样的条件也是多变的
- 这样的服务也有很多。


## 四、想要实现的功能的蓝图
- 每个客户的业务流程地图，每个客户的业务数据的量级，起点，过程，终点。
- 每条数据都能看到它完整的链路，起点，过程，终点。